[package]
name = "jewelcase-server"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = "0.8.9"
jewelcase-core = { version = "0.1.0", path = "../core" }
tokio = { version = "1.53.1", features = ["full"] }
